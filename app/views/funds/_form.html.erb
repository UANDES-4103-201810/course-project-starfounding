<%= form_with(model: fund, local: true) do |form| %>
  <% if fund.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fund.errors.count, "error") %> prohibited this fund from being saved:</h2>

      <ul>
      <% fund.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :fund_product_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :fund_user_id %>
  </div>

  <div class="field">
    <%= form.label :amountFunded %>
    <%= form.text_field :amountFunded, id: :fund_amountFunded %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
